#include "c.h"
#include <vector>

int main(){
    int n, i;
    std::cin >> n;
    std::vector <C*> pointers(n);
    for (i = 0; i < n; ++i){
        pointers[i] = new C();
    }
    for (i = n - 1; i >= 0; --i){
        delete pointers[i];
    }
}
